# playground

Simple browser Snake game with a configurable number of AI snakes. Open `index.html` in a browser to play.

Play online: https://file-diff.github.io/playground/

## GitHub Actions

The repository includes a **Capture Snake game photo** workflow. It starts the game in a browser, plays it briefly, captures a photo, and uploads the generated PNG as a workflow artifact.
